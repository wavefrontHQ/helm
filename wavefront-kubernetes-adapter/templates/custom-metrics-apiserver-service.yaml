apiVersion: v1
kind: Service
metadata:
  name: {{ include "wavefront-kubernetes-adapter.fullname" . }}
  labels:
{{ include "wavefront-kubernetes-adapter.labels" . | indent 4 }}
spec:
  ports:
  - port: 443
    targetPort: 6443
  selector:
    app.kubernetes.io/name: {{ include "wavefront-kubernetes-adapter.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
